import 'package:flutter/material.dart';
import 'analytics_redesign_scaffold.dart';

class AnalyticsRedesignScreen extends StatelessWidget {
  const AnalyticsRedesignScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const AnalyticsRedesignScaffold();
  }
}